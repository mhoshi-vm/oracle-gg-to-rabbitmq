version: '3.8'

services:
  # 1. Source Database: Oracle DB Free
  oracledb:
    image: container-registry.oracle.com/database/free:latest
    container_name: oracle-db
    environment:
      - ORACLE_PWD=OraclePassword1!
    ports:
      - "1521:1521"
    networks:
      - gg_network

  # 2. Target Message Broker: RabbitMQ 4.3
  rabbitmq:
    image: rabbitmq:4.3-management
    container_name: rabbitmq
    ports:
      - "5672:5672"   # AMQP
      - "5552:5552"   # Stream Protocol
      - "15672:15672" # Management UI
    volumes:
      - ./enabled_plugins:/etc/rabbitmq/enabled_plugins
      - ./rabbitmq_config/rabbitmq.conf:/etc/rabbitmq/rabbitmq.conf
      - ./rabbitmq_config/definitions.json:/etc/rabbitmq/definitions.json
    networks:
      - gg_network

  # 3. CDC Engine: Oracle GoldenGate for Big Data
  goldengate:
    image: container-registry.oracle.com/goldengate/goldengate-distributed-apps-and-analytics:latest
    container_name: gg-bigdata
    environment:
      - OGG_ADMIN_PWD=GgAdminPassword1!
    ports:
      - "9000:9000"
      - "9001:9001"
      - "9002:9002"
      - "9003:9003"
    volumes:
      - ./gg_jars:/u01/app/ogg/jars
      - ./gg_config:/u01/app/ogg/config
    depends_on:
      - oracledb
      - rabbitmq
    networks:
      - gg_network

networks:
  gg_network:
    driver: bridge